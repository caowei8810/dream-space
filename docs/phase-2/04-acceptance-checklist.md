# Phase 2 验收清单

## P2-0 文档基线

- [x] 今天确认的用户、审核、配置、计费、套餐、运营、灵感和系统要求均已落文档。
- [x] Phase 1 保持冻结，Phase 2 增量范围和来源优先级明确。
- [x] `development-plan.md` 是唯一执行计划，无相互竞争的新任务编号。
- [x] README、商用就绪和管理端文档不存在“不支持支付”等过时结论。
- [x] 文档链接、阶段编号和 Markdown 格式检查通过。

## P2-1 后台基础

- [x] 新导航与 Phase 2 信息架构一致，桌面和移动端无重叠。
- [x] 运营总览使用真实聚合 API，无前端伪造指标。
- [x] 管理员、角色、权限和会话由服务端 RBAC 控制。

### P2-1 自测证据（待用户验收）

- `pnpm check` 通过：脚本语法、Lint、类型检查、全部单测与生产构建均成功。
- API 单测：运营总览覆盖北京时间日界线、空数据、成功率和收入待接入语义；角色覆盖负责人保护、自身角色保护、关联管理员阻断和权限校验。
- 物理机冒烟：迁移 `20260816160000_add_phase2_admin_permissions` 已部署；`pnpm admin:smoke` 通过，包含总览、角色权限、权限隔离和既有管理链路。
- 浏览器：`1440px` 与 `390px` 下的总览、角色页无横向溢出或重叠；角色创建弹窗可显示，控制台无错误。

用户验收通过前，P2-1 不提交 PR。

## P2-2 用户与风控

- [x] 前台注册用户立即在后台可见。（API smoke：验证码登录后查询 `/admin/users`）
- [x] 限制、封禁、解封和撤销会话在 API 生效。（既有用户生命周期 smoke + 本切片自动封禁会话失效）
- [x] 敏感规则命中可拒绝、限制、封禁或转人工，保存规则版本。（关键词/正则/转人工单测，物理机拒绝与封禁 smoke）

### P2-2 自测证据（待用户验收）

- 迁移：`20260818090000_add_risk_rules_and_hits`、`20260818100000_split_risk_rule_permissions`。
- API：66 项单测通过；规则发布、权限隔离、生成前拒绝、额度不预扣、自动封禁和会话失效已在物理机验证。
- 管理端：提示词风控页面仅通过 `/admin/risk/rules`、`/admin/risk/hits` 及规则写入 API；草稿、发布、归档受 `risk-rules:write` / `risk-rules:publish` 服务端 RBAC 保护。
- 隐私：命中表保存输入哈希与长度，不保存完整提示词；规则下线采用归档，不删除命中证据。

用户验收通过前，P2-2 不提交 PR；P2-3 进入准入条件为图片输入/输出自动审核、人工审核并发领取、申诉复审和未通过结果资产保护。

## P2-3 审核

- [ ] 自动审核覆盖输入和输出，通过/拒绝/可疑路径可复现。
- [ ] 人工领取、决定、并发保护和申诉复审全链路通过。
- [ ] 未审核通过的图片不可下载、公开或进入灵感精选。

## P2-4 计费

- [ ] 标准价 `0.10 元/张` 和五折活动计算正确，历史任务保存价格快照。
- [ ] 无套餐用户按现金余额原子预扣，余额不足不入队。
- [ ] 并发、重复请求、失败返还和混合扣款不产生负数或重复流水。

## P2-5 套餐

- [ ] `10 元 / 500 张` 购买后准确增加 500 张权益。
- [ ] 多图和并发任务按张预扣/结算，套餐不足部分按规则现金补扣。
- [ ] 套餐快照、过期、退款、赠送和人工调整可追溯且幂等。

## P2-6 模型配置

- [ ] 模型能力、参数、路由、灰度、健康检查和回滚真实生效。
- [x] 供应商密钥不返回浏览器，历史任务保存模型配置快照。（本地自测通过，待用户验收）

## P2-7 运营与内容

- [ ] 生图量、时延、用户分布、收入和退款口径可从明细复核。
- [ ] 公告草稿、定时发布、下线和用户端展示闭环通过。
- [x] 灵感只能从审核通过的用户生成结果发布，后台无独立新建入口。（自测通过，待用户验收）

## P2-8 系统与上线

- [ ] 定时清理支持 dry-run、租约、分批、重试、引用保护和恢复。
- [ ] 账务、支付、审核证据和审计日志满足独立保留策略。
- [ ] 全量安全、负载、备份恢复、灰度、回滚和生产上线门禁通过。

每个阶段完成后先记录自动化与物理机自测证据，再通知用户验收；用户验收通过后才能提交 PR。
